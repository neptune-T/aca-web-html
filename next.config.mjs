/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/plote-homepage',
  assetPrefix: '/plote-homepage',
  images: {
    unoptimized: true,
  },
};

export default nextConfig; 